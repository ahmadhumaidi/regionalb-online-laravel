<x-layouts.app title="Jadwal Personalia" active="jadwal-personalia">
    @if (session('status'))<div class="mb-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif
    @if (!empty($snapshot['last_sync_errors']) || !empty($snapshot['errors']))<div class="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{{ implode(' - ', array_map('strval', $snapshot['last_sync_errors'] ?? $snapshot['errors'] ?? [])) }}</div>@endif
    <section class="rounded-2xl border border-border bg-surface p-5"><div class="flex flex-wrap items-center justify-between gap-3"><div><h2 class="text-base font-semibold text-ink">Jadwal (cb.web.id - Personalia)</h2><p class="text-sm text-ink-muted">Snapshot terakhir: {{ $snapshot['synced_at'] ?? 'belum ada sinkronisasi' }}</p></div><form method="POST" action="{{ route('jadwal-personalia.sync') }}">@csrf<button class="rounded-lg bg-brand-600 px-4 py-2 text-sm font-semibold text-white">Sinkronisasi Jadwal</button></form></div><div class="mt-4 flex flex-wrap gap-2 text-xs text-ink-muted"><span class="rounded-full bg-surface-muted px-3 py-1"><b>Zona</b> 2 (Regional B)</span><span class="rounded-full bg-surface-muted px-3 py-1"><b>Diambil</b> {{ $zone['fetched_at'] ?? '-' }}</span>@if (!empty($zone['source_url']))<a class="rounded-full bg-surface-muted px-3 py-1 text-brand-600 underline" href="{{ $zone['source_url'] }}" target="_blank" rel="noopener">Buka sumber asli</a>@endif</div><div class="mt-5 overflow-x-auto">@if (empty($zone['table_html']))<table class="w-full text-sm"><tbody><tr><td class="py-8 text-center text-ink-muted">Belum ada data tersinkron. Klik Sinkronisasi Jadwal.</td></tr></tbody></table>@else{!! $zone['table_html'] !!}@endif</div></section>
</x-layouts.app>
