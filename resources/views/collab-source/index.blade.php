<x-layouts.app title="Sumber Data Collab" active="sumber-collab">
    @if(session('status'))<div class="mb-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif
    <section class="rounded-2xl border border-border bg-surface p-5"><div class="flex flex-wrap items-center justify-between gap-3"><div><h2 class="text-base font-semibold text-ink">Sumber Data Collab (cb.web.id)</h2><p class="text-sm text-ink-muted">Snapshot terakhir: {{ $snapshot['synced_at'] ?: 'belum ada sinkronisasi' }}</p></div><form method="POST" action="{{ route('sumber-collab.sync') }}">@csrf<button class="rounded-lg bg-brand-600 px-4 py-2 text-sm font-semibold text-white">Segarkan Snapshot</button></form></div><div class="mt-4 flex flex-wrap gap-2">@foreach($reports as $name=>$item)<a href="{{ route('sumber-collab',['report'=>$name]) }}" class="rounded-full px-3 py-1 text-xs {{ $name===$active?'bg-brand-600 text-white':'bg-surface-muted text-ink' }}">{{ $name }}</a>@endforeach</div></section>
    @if($report)<section class="mt-5 rounded-2xl border border-border bg-surface p-5"><div class="flex flex-wrap gap-2 text-xs text-ink-muted"><span class="rounded-full bg-surface-muted px-3 py-1"><b>Mode</b> {{ $report['source_mode'] ?: '-' }}</span><span class="rounded-full bg-surface-muted px-3 py-1"><b>Ukuran</b> {{ $report['row_count'] }} baris x {{ $report['column_count'] }} kolom</span><span class="rounded-full bg-surface-muted px-3 py-1"><b>Cache</b> {{ $report['cached_at'] ?: '-' }}</span>@if($report['source_url'])<a class="rounded-full bg-surface-muted px-3 py-1 text-brand-600 underline" href="{{ $report['source_url'] }}" target="_blank" rel="noopener">Buka sumber asli</a>@endif</div><div class="mt-4 overflow-x-auto"><table class="min-w-[1000px] w-full text-left text-xs"><tbody>@foreach($report['rows'] as $row)<tr class="border-b border-border/60">@foreach((array)$row as $cell)<td class="px-2 py-2 align-top">{{ is_scalar($cell)?$cell:json_encode($cell,JSON_UNESCAPED_UNICODE) }}</td>@endforeach</tr>@endforeach</tbody></table></div></section>@endif
</x-layouts.app>
