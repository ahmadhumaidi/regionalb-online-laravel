<x-layouts.app title="Monitoring Konten Kampus" active="konten">
    @if(session('status'))<div class="mb-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif @if($errors->any())<div class="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{{ $errors->first() }}</div>@endif
    <section class="mb-5 grid gap-4 lg:grid-cols-2"><form method="POST" action="{{ route('konten.accounts.store') }}" class="rounded-2xl glass-card p-4">@csrf<h2 class="text-sm font-semibold text-ink">Tambah Akun Instagram</h2><div class="mt-3 grid gap-2 sm:grid-cols-2"><input name="wilayah" placeholder="Regional" required class="rounded-lg border-border bg-surface-muted"><input name="unit_name" placeholder="Unit/Kampus" required class="rounded-lg border-border bg-surface-muted"><input name="instagram_username" placeholder="@username" required class="rounded-lg border-border bg-surface-muted"><input name="pic_name" placeholder="PIC" class="rounded-lg border-border bg-surface-muted"><button class="rounded-lg bg-brand-600 px-3 py-2 text-sm font-semibold text-white sm:col-span-2">Simpan akun</button></div></form><form method="POST" action="{{ route('konten.posts.store') }}" class="rounded-2xl glass-card p-4">@csrf<h2 class="text-sm font-semibold text-ink">Catat Post Manual</h2><div class="mt-3 grid gap-2 sm:grid-cols-2"><select name="account_id" required class="rounded-lg border-border bg-surface-muted"><option value="">Pilih akun</option>@foreach($accounts as $account)<option value="{{ $account['id'] }}">{{ $account['unit_name'] }} · {{ $account['instagram_username'] }}</option>@endforeach</select><input type="date" name="post_date" value="{{ now()->format('Y-m-d') }}" required class="rounded-lg border-border bg-surface-muted"><select name="media_type" class="rounded-lg border-border bg-surface-muted"><option value="feed">Feed</option><option value="reels">Reels</option><option value="story">Story</option><option value="no_post">Tidak posting</option></select><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="keyword_match" value="1"> Memuat keyword PMB</label><input name="post_url" placeholder="URL post (opsional)" class="rounded-lg border-border bg-surface-muted sm:col-span-2"><button class="rounded-lg bg-brand-600 px-3 py-2 text-sm font-semibold text-white sm:col-span-2">Catat post</button></div></form></section>
    <x-konten.filter-bar :filters="$filters" :reference-options="$referenceOptions" />
    <x-dashboard.summary-cards :cards="$summaryCards" />
    <x-konten.regional-table :regionals="$regionals" />
    <x-konten.posts-table :posts="$posts" />
</x-layouts.app>
