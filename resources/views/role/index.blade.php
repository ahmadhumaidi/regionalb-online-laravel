<x-layouts.app title="Peran & Log Aktivitas" active="role">
    <section class="rounded-2xl glass-card p-5"><h2 class="text-base font-semibold text-ink">Aturan akses operasional</h2><div class="mt-4 grid gap-3 md:grid-cols-2 lg:grid-cols-3">@foreach($roles as $role)<article class="rounded-xl border border-border/70 bg-surface-muted p-4"><h3 class="font-semibold text-ink">{{ $role['label'] }}</h3><ul class="mt-2 list-disc space-y-1 pl-5 text-sm text-ink-muted">@foreach($role['points'] as $point)<li>{{ $point }}</li>@endforeach</ul></article>@endforeach</div></section>
    <section class="mt-5 overflow-x-auto rounded-2xl glass-card p-5"><h2 class="mb-3 text-base font-semibold text-ink">Log Aktivitas Terbaru</h2><table class="w-full min-w-[800px] text-left text-sm"><thead><tr class="border-b border-border text-xs text-ink-muted"><th class="py-2 pr-3">Waktu</th><th class="py-2 pr-3">Aktor</th><th class="py-2 pr-3">Aksi</th><th class="py-2 pr-3">Status</th><th class="py-2">Catatan</th></tr></thead><tbody>@forelse($logs as $log)<tr class="border-b border-border/60"><td class="py-2 pr-3">{{ $log->created_at }}</td><td class="py-2 pr-3">{{ $log->actor_name ?: '-' }}</td><td class="py-2 pr-3">{{ $log->action_name }}</td><td class="py-2 pr-3">{{ $log->old_status ?: '-' }} → {{ $log->new_status ?: '-' }}</td><td class="py-2">{{ $log->note ?: '-' }}</td></tr>@empty<tr><td colspan="5" class="py-8 text-center text-ink-muted">Belum ada log aktivitas.</td></tr>@endforelse</tbody></table></section>
</x-layouts.app>
